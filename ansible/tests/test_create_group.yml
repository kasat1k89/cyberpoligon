---
- name: Verify create_group
  hosts: "{{ target_host }}"
  become: true
  gather_facts: false

  tasks:
    - name: Read group entry
      ansible.builtin.getent:
        database: group
        key: "{{ group_name }}"
      register: grp
      tags: always
